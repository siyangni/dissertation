# Ignore everything by default
*
*/

# But track these files and directories
!.gitignore

# Allow scripts directory and all R script files
!scripts/
!scripts/**
scripts/**/*
!scripts/**/*.R

# Allow R scripts in root directory (if any)
!*.R

# Allow README and documentation files (optional - remove if not needed)
!README.md
!README.txt
